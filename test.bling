typedef String struct {
    _str charptr
    _len int
}

fun newString(str charptr) String {
    var s String = {
        _str: sys$strdup(str),
        _len: strlen(str),
    }
    return s
}

// fun (s *String) len() {
//     return s._len
// }
